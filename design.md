# DocViewer & Editor - 모바일 앱 설계 문서

## 개요
한글(HWP), PDF, 엑셀(XLSX), PPTX 파일을 보고 편집할 수 있는 안드로이드 앱입니다. 간단한 편집 기능(메모, 잘라내기, 화면 크기 조정)과 파일 변환 기능을 제공합니다.

---

## 화면 구조 (Screen List)

### 1. **홈 화면 (Home)**
- 최근 열어본 파일 목록
- 파일 찾아보기 버튼
- 파일 추가/가져오기 버튼
- 파일 검색 기능

### 2. **파일 브라우저 (File Browser)**
- 기기의 파일 시스템 탐색
- 지원 파일 형식 필터링 (HWP, PDF, XLSX, PPTX)
- 파일 선택 및 열기
- 최근 파일 섹션

### 3. **뷰어 화면 (Document Viewer)**
- 선택한 파일 표시 (PDF, PPTX, XLSX, HWP)
- 페이지/시트 네비게이션
- 줌 인/아웃 (핀치 제스처 또는 버튼)
- 화면 회전 지원
- 상단 도구 모음 (편집, 메모, 설정)

### 4. **편집 모드 (Edit Mode)**
- **메모 추가**: 문서에 텍스트 메모 삽입
- **자르기**: 특정 영역 선택 및 잘라내기
- **화면 크기 조정**: 줌 레벨 및 레이아웃 조정
- **저장**: 편집된 내용 저장

### 5. **파일 변환 (File Conversion)**
- 지원 형식: PDF → 이미지, PPTX → PDF, XLSX → PDF, HWP → PDF
- 변환 진행 표시
- 변환된 파일 저장 위치 선택

### 6. **설정 화면 (Settings)**
- 앱 테마 (라이트/다크 모드)
- 기본 줌 레벨
- 파일 저장 위치
- 앱 정보

---

## 주요 콘텐츠 및 기능

### 홈 화면
- **최근 파일 카드**: 파일명, 미리보기 이미지, 마지막 수정 날짜
- **빠른 작업**: "파일 열기", "파일 추가" 버튼
- **검색 바**: 파일명으로 검색

### 뷰어 화면
- **문서 표시 영역**: 파일 내용 렌더링
- **페이지 인디케이터**: "페이지 3/10" 형식
- **네비게이션 버튼**: 이전/다음 페이지
- **도구 모음**: 메모, 자르기, 줌, 회전, 변환

### 편집 모드
- **메모 도구**: 텍스트 입력 필드, 메모 위치 선택
- **자르기 도구**: 드래그로 영역 선택, 미리보기
- **줌 컨트롤**: 슬라이더 또는 +/- 버튼
- **저장 버튼**: 편집 내용 적용

### 파일 변환 화면
- **원본 파일 정보**: 파일명, 크기, 형식
- **변환 옵션**: 출력 형식 선택, 품질 설정
- **변환 진행률**: 프로그레스 바
- **저장 위치**: 파일 저장 경로 선택

---

## 주요 사용자 흐름 (Key User Flows)

### 흐름 1: 파일 열기
1. 홈 화면에서 "파일 열기" 탭
2. 파일 브라우저 열기
3. 파일 선택
4. 뷰어 화면에서 파일 표시

### 흐름 2: 문서에 메모 추가
1. 뷰어 화면에서 "메모" 버튼 탭
2. 메모 추가 모드 활성화
3. 메모 텍스트 입력
4. 메모 위치 선택 (페이지 상의 좌표)
5. 메모 저장

### 흐름 3: 문서 자르기
1. 뷰어 화면에서 "자르기" 버튼 탭
2. 자르기 영역 선택 (드래그)
3. 미리보기 확인
4. "적용" 버튼으로 저장

### 흐름 4: 파일 변환
1. 뷰어 화면에서 "변환" 버튼 탭
2. 변환 대상 형식 선택
3. 변환 옵션 설정 (품질, 해상도 등)
4. "변환 시작" 버튼
5. 진행률 표시
6. 변환 완료 후 저장 위치 선택

### 흐름 5: 화면 크기 조정
1. 뷰어 화면에서 줌 컨트롤 사용
2. 핀치 제스처로 확대/축소
3. 또는 +/- 버튼으로 조정
4. 설정에서 기본 줌 레벨 저장

---

## 색상 선택 (Color Scheme)

| 요소 | 색상 | 용도 |
|------|------|------|
| **Primary** | #0066CC (파란색) | 주요 버튼, 강조 요소 |
| **Background** | #FFFFFF (라이트) / #1A1A1A (다크) | 화면 배경 |
| **Surface** | #F5F5F5 (라이트) / #2D2D2D (다크) | 카드, 패널 배경 |
| **Text** | #1A1A1A (라이트) / #FFFFFF (다크) | 주 텍스트 |
| **Muted** | #666666 (라이트) / #999999 (다크) | 보조 텍스트 |
| **Success** | #22C55E (초록색) | 성공 메시지 |
| **Warning** | #F59E0B (주황색) | 경고 메시지 |
| **Error** | #EF4444 (빨간색) | 오류 메시지 |
| **Border** | #E0E0E0 (라이트) / #404040 (다크) | 구분선 |

---

## 기술 스택

- **프레임워크**: React Native + Expo 54
- **상태 관리**: React Context + AsyncStorage
- **파일 처리**: expo-document-picker, expo-file-system
- **PDF 렌더링**: react-native-pdf (또는 대체 라이브러리)
- **PPTX/XLSX 렌더링**: 웹 기반 라이브러리 (SheetJS, pptxjs)
- **HWP 렌더링**: 네이티브 모듈 또는 변환 서비스
- **UI**: NativeWind (Tailwind CSS)
- **스타일**: Tailwind CSS

---

## 주요 기술 고려사항

1. **파일 형식 지원**
   - PDF: react-native-pdf 라이브러리 사용
   - PPTX: pptxjs 또는 유사 라이브러리
   - XLSX: SheetJS (xlsx 라이브러리)
   - HWP: 변환 서비스 또는 네이티브 모듈 필요

2. **성능**
   - 큰 파일의 경우 페이지/시트 단위로 렌더링
   - 이미지 캐싱으로 성능 최적화
   - 백그라운드에서 변환 작업 처리

3. **저장소**
   - 최근 파일 목록: AsyncStorage
   - 편집된 파일: 기기 파일 시스템
   - 메모 및 주석: 로컬 데이터베이스 (SQLite)

4. **보안**
   - 민감한 파일 암호화 저장 (선택사항)
   - 파일 접근 권한 관리

---

## 향후 확장 기능

- 클라우드 동기화 (Google Drive, OneDrive)
- OCR 기능 (이미지 텍스트 인식)
- 손글씨 주석
- 파일 비교 기능
- 배치 변환
- 파일 암호화
